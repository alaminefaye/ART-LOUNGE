name: Build Windows App

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Get Dependencies
        run: |
          cd resto-app
          flutter pub get

      - name: Build Windows
        run: |
          cd resto-app
          flutter build windows --release

      - name: Package Release
        run: |
          mkdir release-pkg
          xcopy /E /I resto-app\build\windows\x64\runner\Release\* release-pkg\
          powershell Compress-Archive -Path release-pkg\* -DestinationPath Windows-RestoApp.zip

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Windows-RestoApp-Release
          path: Windows-RestoApp.zip
